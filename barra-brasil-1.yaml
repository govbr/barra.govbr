# configuracao do assetgen para:
# - gerar o script de comportamento, minificado
# - gerar o css com as imagens incorporadas

generate:

- barra-brasil.js:
    source:
      - assets/barra-brasil.coffee

- estilo-barra.css:
    source:
      - assets/estilo-barra.scss
    depends:
      - assets/imagens/*
    embed.path.root: assets
    embed.url.base: imagens/

- barra-brasil-css.js:
    source:
      - raw: "barrabrasil.insere_css('"
      - build/estilo-barra.css
      - raw: "');" # fechamento da chamada de funcao

css.embed: true
css.embed.only: true
css.compress: true
js.compress: false
js.uglify.bin: uglifyjs
output.directory: build
output.hashed: false
output.manifest: app/assets.json

profile.dev:
  css.compress: false
  js.compress: false
